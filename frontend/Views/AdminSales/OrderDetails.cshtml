@model frontend.Models.Order

@{
    ViewBag.Title = "Chi tiết đơn hàng #" + Model.Id;
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="container" style="padding-top: 30px; padding-bottom: 50px;">
    <div style="margin-bottom: 20px;">
        <a href="@Url.Action("Index", "AdminOrders")" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left"></i> Quay lại lịch sử
        </a>
    </div>

    @Html.Partial("_OrderInvoice_Admin", Model)
</div>

